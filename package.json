{
  "name": "accessforge",
  "version": "0.1.0",
  "private": true,
  "description": "Co-design tools for personalized low-risk assistive adapters",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "pnpm --filter @accessforge/web dev",
    "build": "pnpm generate:api && pnpm --filter @accessforge/web build",
    "lint": "pnpm --filter @accessforge/web lint",
    "typecheck": "pnpm --filter @accessforge/web typecheck",
    "test": "pnpm --filter @accessforge/web test",
    "format:check": "prettier --check .",
    "generate:api": "uv run --project services/api python scripts/export_openapi.py > packages/api-client/openapi.json && openapi-typescript packages/api-client/openapi.json -o packages/api-client/src/generated.ts",
    "dev:keys": "node scripts/generate-dev-keys.mjs"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "prettier": "^3.9.6"
  }
}
